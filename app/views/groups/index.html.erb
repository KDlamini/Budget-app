<h1>Transactions</h1>

<div id="groups">
  <% @groups.each do |group| %>
    <%= link_to group_path(group) do %>
      <%= render group %>
    <% end %>
  <% end %>
</div>

<%= link_to "Add Category", new_group_path %>
